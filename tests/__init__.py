"""WhisperTyper test suite.

Covers the Qt-free layers (app/core, app/audio/store, app/services/net & rephrasing
helpers), so the tests run headless — no display, audio hardware or GUI toolkit needed.
"""
